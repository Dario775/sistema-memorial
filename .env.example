# 🔐 Variables de Entorno - Sistema Memorial
# Copia este archivo como .env y configura los valores apropiados

# Configuración del Servidor
NODE_ENV=production
PORT=3000

# Seguridad JWT
# IMPORTANTE: Genera una clave segura de al menos 64 caracteres
# Puedes usar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_clave_jwt_super_secreta_de_64_caracteres_minimo_cambiala_ahora

# Configuración de Base de Datos (Futuro)
# DATABASE_URL=postgresql://usuario:password@host:puerto/database

# Configuración de Email (Futuro)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@gmail.com
# SMTP_PASS=tu_password_de_aplicacion

# Configuración de Almacenamiento (Futuro)
# CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Configuración de Logs
# LOG_LEVEL=info
